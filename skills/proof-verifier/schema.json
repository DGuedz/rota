{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Proof Verifier Schema",
  "type": "object",
  "required": ["proofPayload", "signatures"],
  "properties": {
    "proofPayload": {
      "type": "string",
      "description": "The base64 or hex encoded proof payload to verify."
    },
    "escrowId": {
      "type": "string",
      "description": "Optional Escrow ID associated with the proof to check format constraints."
    },
    "signatures": {
      "type": "array",
      "description": "Array of signatures attached to the proof.",
      "items": {
        "type": "object",
        "required": ["signer", "signature"],
        "properties": {
          "signer": {
            "type": "string",
            "description": "Public key or address of the signer."
          },
          "signature": {
            "type": "string",
            "description": "Cryptographic signature of the proof payload."
          }
        }
      }
    }
  }
}
